function max = getMaxAverage(A)
%GETMAX Summary of this function goes here
%   Detailed explanation goes here
a_old = 0;
N = 0;
max = 0;
for i = 1:size(A)
    if ( abs(A(i)) < abs(a_old))
        N = N +1;
        max = max + abs(A(i))
    end
    a_old = A(i);
end

